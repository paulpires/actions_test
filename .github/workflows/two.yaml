name: jobtwo
on:
  pull_request:
    types: [ closed ]
    branches: [ main ]

  workflow_run:
    workflows: [jobone]
    types:
      - completed

jobs:
  on-success:
    runs-on: ubuntu-latest
    # if: ${{ github.event.workflow_run.conclusion == 'success' }}
    if: (github.event == 'workflow_run') || (github.event == 'pull_request' && github.event.action == 'closed' && github.event.pull_request.merged == true)
    steps:
      - run: echo 'The triggering workflow passed'
  # on-failure:
  #   runs-on: ubuntu-latest
  #   if: ${{ github.event.workflow_run.conclusion == 'failure' }}
  #   steps:
  #     - run: echo 'The triggering workflow failed'


# some change here