name: "1st job"

on:
  pull_request:
    types:
      - opened
      - reopened
      - ready_for_review

jobs:
  job_one:
    runs-on: ubuntu-latest
    steps:
      - name: job number one
        run: |
          echo job ONE done
